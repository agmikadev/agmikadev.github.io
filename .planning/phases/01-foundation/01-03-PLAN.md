---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SideStrip/SideStrip.tsx
  - src/components/SideStrip/SideStrip.css
  - public/resume.pdf
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "User can download resume/CV as PDF from the portfolio"
    - "Download link is visible and accessible from the main UI"
  artifacts:
    - path: "public/resume.pdf"
      provides: "Resume PDF file for download"
    - path: "src/components/SideStrip/SideStrip.tsx"
      provides: "Download button integrated into SideStrip FooterInfo"
      contains: "resume.pdf"
  key_links:
    - from: "src/components/SideStrip/SideStrip.tsx"
      to: "public/resume.pdf"
      via: "anchor tag with download attribute"
      pattern: "href.*resume\\.pdf.*download"
---

<objective>
Add a resume/CV PDF download link to the portfolio UI so visitors can download the portfolio owner's resume.

Purpose: Hiring managers and technical peers need a way to obtain a traditional resume document alongside the interactive 3D portfolio experience.
Output: Resume PDF in public folder with download button in the SideStrip footer area.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/components/SideStrip/SideStrip.tsx
@src/components/SideStrip/SubComponents/FooterInfo.tsx
@src/components/SideStrip/SideStrip.css
</context>

<tasks>

<task type="auto">
  <name>task 1: add resume download button to SideStrip footer</name>
  <files>src/components/SideStrip/SideStrip.tsx, src/components/SideStrip/SideStrip.css</files>
  <read_first>
    - src/components/SideStrip/SideStrip.tsx — current structure with FooterInfo
    - src/components/SideStrip/SubComponents/FooterInfo.tsx — footer component where button will be added
    - src/components/SideStrip/SideStrip.css — existing styles for footer area
    - src/App.css — CSS custom properties for color tokens
  </read_first>
  <action>
    Add a resume download button to the SideStrip footer area. The button should be placed within or alongside the existing FooterInfo component.

    **Option: Modify FooterInfo.tsx directly (preferred for clean separation):**

    In `src/components/SideStrip/SubComponents/FooterInfo.tsx`, add an anchor element styled as a button:

    ```tsx
    <a
      href="/star-system-portfolio/resume.pdf"
      download="Mikael-Angelo-Resume.pdf"
      className="resume-download-btn"
      aria-label="Download resume as PDF"
    >
      <span className="btn-icon">↓</span>
      <span className="btn-text">DOWNLOAD CV</span>
    </a>
    ```

    The href path must be `/star-system-portfolio/resume.pdf` to match the GitHub Pages base path configured in Plan 01. For local development, Vite's dev server serves from `/`, so use a relative path approach:

    Actually — use `/resume.pdf` as the href. Vite's dev server serves the public folder at root, and the GitHub Pages base path only affects bundled assets, not public folder files. The `gh-pages` deployment copies the entire `dist/` folder (which includes public files) to the repo root, so `/resume.pdf` works in both environments.

    Correct href: `href="/resume.pdf"`

    **CSS for the button (add to SideStrip.css):**
    ```css
    .resume-download-btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 16px;
      margin-top: 12px;
      background: transparent;
      border: 1px solid var(--ai-glow, #a9fc03);
      color: var(--ai-glow, #a9fc03);
      font-family: monospace;
      font-size: 11px;
      letter-spacing: 1px;
      text-transform: uppercase;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.2s ease;
      border-radius: 4px;
    }
    .resume-download-btn:hover {
      background: rgba(169, 252, 3, 0.1);
      box-shadow: 0 0 12px rgba(169, 252, 3, 0.3);
    }
    ```

    Do NOT create a new component file — add the button to the existing FooterInfo component to maintain the current component hierarchy.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('src/components/SideStrip/SubComponents/FooterInfo.tsx','utf8'); const c=fs.readFileSync('src/components/SideStrip/SideStrip.css','utf8'); console.log(t.includes('resume.pdf') && t.includes('download') && c.includes('resume-download-btn') ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - FooterInfo.tsx contains `<a` element with `href="/resume.pdf"` and `download` attribute (grep confirms both)
    - FooterInfo.tsx contains `aria-label="Download resume as PDF"` (grep confirms)
    - SideStrip.css contains `.resume-download-btn` class definition (grep confirms)
    - CSS includes hover state with `background: rgba(169, 252, 3, 0.1)` (grep confirms)
    - `npx tsc --noEmit --project tsconfig.app.json` passes without errors
  </acceptance_criteria>
  <done>
    FooterInfo includes a styled resume download button with correct href, download attribute, and aria-label. CSS matches portfolio theme. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>task 2: create placeholder resume PDF in public folder</name>
  <files>public/resume.pdf</files>
  <read_first>
    - public/ — verify current public folder contents
  </read_first>
  <action>
    Create a placeholder PDF file at `public/resume.pdf`. This is a temporary placeholder that the user will replace with their actual resume.

    Since we cannot generate a real PDF programmatically without external tools, create a minimal valid PDF file using raw PDF syntax. A minimal PDF structure:

    Write the following content to `public/resume.pdf` as binary-safe content:
    ```
    %PDF-1.0
    1 0 obj<</Type/Catalog/Pages 2 0 R>>endobj
    2 0 obj<</Type/Pages/Kids[3 0 R]/Count 1>>endobj
    3 0 obj<</Type/Page/MediaBox[0 0 612 792]/Parent 2 0 R/Resources<</Font<</F1 4 0 R>>>>/Contents 5 0 R>>endobj
    4 0 obj<</Type/Font/Subtype/Type1/BaseFont/Helvetica>>endobj
    5 0 obj<</Length 44>>stream
    BT /F1 24 Tf 100 700 Td (Resume - Replace with actual PDF) Tj ET
    endstream
    endobj
    xref
    0 6
    0000000000 65535 f 
    0000000009 00000 n 
    0000000052 00000 n 
    0000000101 00000 n 
    0000000214 00000 n 
    0000000294 00000 n 
    trailer<</Size 6/Root 1 0 R>>
    startxref
    388
    %%EOF
    ```

    Use the write tool with the exact bytes above. The file must be a valid PDF that browsers can download.

    Add a comment at the top of the SideStrip download button in FooterInfo.tsx noting this is a placeholder:
    ```tsx
    {/* TODO: Replace public/resume.pdf with actual resume PDF */}
    ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const buf=fs.readFileSync('public/resume.pdf'); console.log(buf.slice(0,5).toString()==='%PDF-' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `public/resume.pdf` exists and starts with `%PDF-` header (node -e confirms first 5 bytes)
    - File size is greater than 100 bytes (valid PDF, not empty)
    - FooterInfo.tsx contains a TODO comment about replacing the placeholder (grep confirms)
    - `npm run build` succeeds and dist/ contains resume.pdf (Test-Path dist/resume.pdf after build)
  </acceptance_criteria>
  <done>
    Valid placeholder PDF exists in public folder, is copied to dist/ on build, and FooterInfo has a TODO comment reminding the user to replace it with their actual resume.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → public/resume.pdf | Static file download; no server-side processing |
| User click → download | Client-side anchor tag with download attribute; no external requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | public/resume.pdf | accept | Static file in source control; no runtime modification; user replaces with their own PDF |
| T-01-06 | Information Disclosure | Resume PDF content | accept | Public portfolio intentionally shares resume; no PII beyond what user chooses to include |
</threat_model>

<verification>
- Run `npm run build` and verify `dist/resume.pdf` exists
- In dev mode (`npm run dev`), click the download button and verify PDF downloads
- Verify the `download` attribute sets the filename to "Mikael-Angelo-Resume.pdf"
- Confirm the button is visible in the SideStrip footer area
</verification>

<success_criteria>
- [ ] Resume download button exists in FooterInfo with correct href and download attribute
- [ ] Button styled with portfolio theme colors (ai-glow accent)
- [ ] public/resume.pdf exists as a valid PDF file
- [ ] dist/resume.pdf exists after build
- [ ] Button has aria-label for accessibility
- [ ] FOUND-04 requirement is satisfied: user can download resume/CV as PDF
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
