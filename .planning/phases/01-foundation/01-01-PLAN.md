---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - package.json
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "Portfolio loads on GitHub Pages without 404 errors on assets"
    - "All JS/CSS bundles reference correct base path"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite base path configuration for GitHub Pages"
      contains: "base: '/star-system-portfolio/'"
    - path: "package.json"
      provides: "Homepage URL for gh-pages deployment"
      contains: "homepage"
  key_links:
    - from: "vite.config.ts"
      to: "package.json"
      via: "base path must match repo name in homepage URL"
      pattern: "base.*homepage"
---

<objective>
Fix GitHub Pages deployment so the portfolio loads without 404 errors on JS/CSS assets.

Purpose: The current vite.config.ts has `base: '/'` which causes asset 404s when deployed to GitHub Pages under a repository subpath (e.g., `username.github.io/repo-name/`).
Output: Correctly configured Vite base path and package.json homepage URL.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@vite.config.ts
@package.json
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>task 1: fix vite base path for GitHub Pages</name>
  <files>vite.config.ts</files>
  <read_first>
    - vite.config.ts — current base path configuration
    - package.json — homepage field for repo name extraction
  </read_first>
  <action>
    Edit vite.config.ts to set the `base` property to match the GitHub Pages repository path.

    Current value: `base: '/'`
    Required value: `base: '/star-system-portfolio/'`

    The repository name is derived from the package.json `homepage` field (`https://agmikadev.github.io`) combined with the package `name` field (`star-system-portfolio`). The full GitHub Pages URL will be `https://agmikadev.github.io/star-system-portfolio/`.

    The deploy.yml workflow uses `actions/configure-pages@v4` which auto-detects the base path from the repository context, but the vite.config.ts `base` must still match for local preview (`npm run preview`) and for the built HTML to reference assets correctly.

    Change:
    ```typescript
    // FROM:
    base: '/',
    // TO:
    base: '/star-system-portfolio/',
    ```

    Do NOT change the plugins array or any other config. Only modify the `base` value.
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "dist" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts contains `base: '/star-system-portfolio/'` (grep confirms exact string)
    - `npm run build` completes without errors (exit code 0)
    - dist/index.html contains `<script type="module" crossorigin src="/star-system-portfolio/assets/` (grep confirms path prefix)
    - dist/index.html does NOT contain `<script` with `src="/assets/` (grep -v confirms no root-relative paths)
  </acceptance_criteria>
  <done>
    Vite base path set to '/star-system-portfolio/', build succeeds, and all asset references in dist/index.html use the correct subpath prefix.
  </done>
</task>

<task type="auto">
  <name>task 2: verify package.json homepage and deploy workflow alignment</name>
  <files>package.json</files>
  <read_first>
    - package.json — homepage and name fields
    - .github/workflows/deploy.yml — deployment configuration
    - vite.config.ts — base path (modified in task 1)
  </read_first>
  <action>
    Verify that package.json `homepage` field is correctly set for GitHub Pages deployment via gh-pages.

    Current homepage: `"https://agmikadev.github.io"` — this is the user domain, not the repo-specific URL.

    The `gh-pages` deploy script (`npm run deploy` → `gh-pages -d dist`) uses the homepage field to determine the remote URL. For a repo at `github.com/agmikadev/star-system-portfolio`, the homepage should be:
    `"https://agmikadev.github.io/star-system-portfolio/"`

    Update package.json homepage from:
    ```json
    "homepage": "https://agmikadev.github.io",
    ```
    To:
    ```json
    "homepage": "https://agmikadev.github.io/star-system-portfolio/",
    ```

    Verify deploy.yml does not need changes — it uses `actions/deploy-pages@v4` which handles GitHub Pages deployment via the Pages API, independent of the homepage field. The gh-pages npm script is an alternative deployment method.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); console.log(p.homepage === 'https://agmikadev.github.io/star-system-portfolio/' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json homepage is exactly `"https://agmikadev.github.io/star-system-portfolio/"` (node -e confirms)
    - `npm run build` still succeeds after homepage change (exit code 0)
    - deploy.yml remains unchanged (git diff shows no changes to .github/workflows/deploy.yml)
  </acceptance_criteria>
  <done>
    package.json homepage matches the full GitHub Pages URL, build succeeds, and deploy workflow is confirmed aligned.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages → Browser | Static assets served from GitHub CDN; no server-side processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | vite.config.ts | accept | Build config is source-controlled; no runtime input; low-value target |
| T-01-02 | Information Disclosure | package.json homepage | accept | Public URL only; no secrets or credentials exposed |
</threat_model>

<verification>
- Run `npm run build` — must complete without errors
- Inspect `dist/index.html` — all script and link tags must reference `/star-system-portfolio/assets/` paths
- Verify no root-relative (`/assets/`) or relative (`./assets/`) asset paths remain in built output
</verification>

<success_criteria>
- [ ] vite.config.ts base path is `/star-system-portfolio/`
- [ ] package.json homepage is `https://agmikadev.github.io/star-system-portfolio/`
- [ ] `npm run build` produces dist/ with correct asset paths
- [ ] dist/index.html references use `/star-system-portfolio/` prefix for all assets
- [ ] FOUND-01 requirement is satisfied: GitHub Pages deployment will resolve assets correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
