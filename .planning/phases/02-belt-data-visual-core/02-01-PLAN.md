---
phase: 02-belt-data-visual-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Planetary/data/BeltData.ts
  - src/components/Planetary/data/index.ts
autonomous: true
requirements:
  - BELT-02

must_haves:
  truths:
    - "BeltData.ts exports BeltTool, BeltMissionReport, and BeltDataModel types"
    - "beltDataModel constant contains AI tools with proficiency percentages"
    - "beltDataModel contains at least 3 mission reports with case-study structure"
    - "data/index.ts re-exports all BeltData types and constants"
  artifacts:
    - path: "src/components/Planetary/data/BeltData.ts"
      provides: "Belt data model with AI tools, proficiency metrics, and mission reports"
      exports: ["BeltTool", "BeltMissionReport", "BeltDataModel", "beltDataModel"]
    - path: "src/components/Planetary/data/index.ts"
      provides: "Re-exports BeltData module"
      contains: "export * from './BeltData'"
  key_links:
    - from: "src/components/Planetary/data/BeltData.ts"
      to: "src/components/Planetary/data/index.ts"
      via: "barrel export"
      pattern: "export \\* from.*BeltData"
---

<objective>
Create dedicated belt data model (BeltData.ts) with TypeScript types and static constants for AI/Agentic tools, proficiency percentages, and mission reports — separate from planet data.

Purpose: BELT-02 requires a belt data model containing AI/Agentic tools with proficiency percentages and mission reports. Currently the belt planet entry in PlanetaryData.ts has generic tools/stats but no belt-specific structured data. This plan creates the canonical data source that Phase 3 (BeltHUD) will consume.
Output: BeltData.ts with typed exports, integrated into data barrel.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/components/Planetary/data/PlanetaryData.ts
@src/components/Planetary/data/MissionData.ts
@src/components/Planetary/data/index.ts
</context>

<interfaces>
<!-- Key types from existing data files that BeltData should follow the same pattern as. -->

From src/components/Planetary/data/PlanetaryData.ts:
```typescript
export interface TelemetryStat {
  label: string;
  value: number | string;
  alternatives?: string[];
}

export interface PlanetModel {
  id: string;
  name: string;
  type: string;
  description: string;
  tools: string[];
  shape: "sphere" | "hexagon" | "icosahedron" | "belt";
  variant?: "standard" | "ringed" | "wireframe";
  hasAI?: boolean;
  color: string;
  textureUrl?: string;
  size: number;
  orbitSize: number;
  speed: number;
  stats: TelemetryStat[];
}
```

From src/components/Planetary/data/MissionData.ts:
```typescript
export interface MissionType {
  id: string;
  title: string;
  year: string;
  role: string;
  summary: string;
  technical_briefing: string;
  technologies: string[];
  external_links?: { label: string; url: string }[];
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>task 1: create BeltData.ts with types and static data</name>
  <files>src/components/Planetary/data/BeltData.ts</files>
  <read_first>
    - src/components/Planetary/data/PlanetaryData.ts (pattern reference for interface style)
    - src/components/Planetary/data/MissionData.ts (pattern reference for mission structure)
  </read_first>
  <action>
Create `src/components/Planetary/data/BeltData.ts` with the following exports:

**1. BeltTool interface:**
```typescript
export interface BeltTool {
  id: string;
  name: string;
  category: "llm" | "agent-framework" | "orchestration" | "evaluation" | "rag" | "mcp" | "prompt-engineering";
  proficiency: number; // 0-100 percentage
  description: string;
  relatedTechnologies: string[]; // alternative/similar tools
}
```

**2. BeltMissionReport interface:**
```typescript
export interface BeltMissionReport {
  id: string;
  title: string;
  date: string;
  problem: string;
  approach: string;
  outcome: string;
  toolsUsed: string[]; // IDs referencing BeltTool.id
  metrics: { label: string; value: string }[];
}
```

**3. BeltDataModel interface:**
```typescript
export interface BeltDataModel {
  name: string;
  description: string;
  color: string;
  tools: BeltTool[];
  missionReports: BeltMissionReport[];
}
```

**4. beltDataModel constant** — Populate with real data from the user's existing PlanetaryData.ts belt entry (S.O.N.D.A. NETWORK). Map the existing 13 stats into BeltTool entries with appropriate categories. Create at least 3 BeltMissionReport entries derived from the existing MISSION_ARCHIVE entries that best demonstrate AI/Agentic skills (e.g., the portfolio project with AI optimization, the MaxVidro glass cutting system with algorithmic optimization, and the Portal Egressos with performance optimization).

Use `color: "#a9fc03"` to match the existing belt planet color. The name should be "S.O.N.D.A. NETWORK" and description should match the existing belt planet description from PlanetaryData.ts.

Do NOT modify PlanetaryData.ts or MissionData.ts — this is a new file only.
  </action>
  <verify>
    <automated>node -e "const m = require('./src/components/Planetary/data/BeltData.ts'); console.log('OK')" 2>&1 || npx tsc --noEmit src/components/Planetary/data/BeltData.ts 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Planetary/data/BeltData.ts exists
    - File exports BeltTool interface with fields: id, name, category, proficiency, description, relatedTechnologies
    - File exports BeltMissionReport interface with fields: id, title, date, problem, approach, outcome, toolsUsed, metrics
    - File exports BeltDataModel interface with fields: name, description, color, tools, missionReports
    - File exports beltDataModel constant of type BeltDataModel
    - beltDataModel.tools has at least 8 BeltTool entries
    - beltDataModel.missionReports has at least 3 BeltMissionReport entries
    - beltDataModel.color === "#a9fc03"
    - beltDataModel.name === "S.O.N.D.A. NETWORK"
    - TypeScript compiles without errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>BeltData.ts created with all types and populated static data, compiles cleanly</done>
</task>

<task type="auto" tdd="false">
  <name>task 2: integrate BeltData into data barrel export</name>
  <files>src/components/Planetary/data/index.ts</files>
  <read_first>
    - src/components/Planetary/data/index.ts (current state)
  </read_first>
  <action>
Add `export * from "./BeltData";` to `src/components/Planetary/data/index.ts`. The file currently has two lines exporting PlanetaryData and MissionData. Add the BeltData export as a third line, maintaining the same pattern.

Result should be:
```typescript
export * from "./PlanetaryData";
export * from "./MissionData";
export * from "./BeltData";
```
  </action>
  <verify>
    <automated>grep -c "BeltData" src/components/Planetary/data/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Planetary/data/index.ts contains `export * from "./BeltData"`
    - grep count for "BeltData" in index.ts is exactly 1
    - TypeScript compiles without errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>BeltData is exported through the data barrel, available via `import { beltDataModel } from '../data'`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A (static data) | All data is static TypeScript constants — no user input, no external API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | BeltData.ts | accept | Static portfolio data only (tools, proficiency, mission reports) — no PII, secrets, or sensitive data. Low-value target. |
</threat_model>

<verification>
- BeltData.ts exists with all 3 interfaces and beltDataModel constant
- data/index.ts re-exports BeltData
- TypeScript compilation passes with no errors
- beltDataModel.tools.length >= 8
- beltDataModel.missionReports.length >= 3
- All BeltTool entries have valid category values from the union type
- All BeltMissionReport entries have problem, approach, outcome fields populated (non-empty strings)
</verification>

<success_criteria>
- BELT-02 satisfied: Belt data model exists with AI/Agentic tools, proficiency percentages, and mission reports
- Data is typed and exported through the barrel pattern consistent with existing data modules
- Phase 3 can import beltDataModel directly from '../data' for BeltHUD consumption
</success_criteria>

<output>
After completion, create `.planning/phases/02-belt-data-visual-core/02-01-SUMMARY.md`
</output>
