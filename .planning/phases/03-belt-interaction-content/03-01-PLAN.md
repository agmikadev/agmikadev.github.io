---
phase: "03"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Planetary/UI/Tabs/Belt/BeltMissionsTab.tsx
  - src/components/Planetary/UI/Tabs/Belt/BeltMissionsTab.css
  - src/components/Planetary/UI/Tabs/Belt/BeltMissionCard.tsx
  - src/components/Planetary/UI/Tabs/Belt/BeltMissionCard.css
  - src/components/Planetary/UI/Tabs/Belt/BeltAnalyticsTab.tsx
  - src/components/Planetary/UI/Tabs/Belt/BeltAnalyticsTab.css
  - src/components/Planetary/UI/Tabs/index.ts
autonomous: true
requirements: [BELT-04, CONT-03]

must_haves:
  truths:
    - "BeltMissionsTab renders BeltMissionReport cards with problem → approach → outcome structure"
    - "BeltAnalyticsTab renders BeltTool proficiency bars grouped by category"
    - "BeltMissionCard displays title, date, sections, tools tags, and metrics"
  artifacts:
    - path: "src/components/Planetary/UI/Tabs/Belt/BeltMissionsTab.tsx"
      provides: "Filters and renders belt mission reports"
      exports: ["BeltMissionsTab"]
    - path: "src/components/Planetary/UI/Tabs/Belt/BeltMissionCard.tsx"
      provides: "Card component for BeltMissionReport shape"
      exports: ["BeltMissionCard"]
    - path: "src/components/Planetary/UI/Tabs/Belt/BeltAnalyticsTab.tsx"
      provides: "Grouped tool proficiency bars for belt tools"
      exports: ["BeltAnalyticsTab"]
  key_links:
    - from: "src/components/Planetary/UI/Tabs/Belt/BeltMissionsTab.tsx"
      to: "src/components/Planetary/data/BeltData.ts"
      via: "beltDataModel.missionReports"
      pattern: "beltDataModel\\.missionReports"
    - from: "src/components/Planetary/UI/Tabs/Belt/BeltAnalyticsTab.tsx"
      to: "src/components/Planetary/data/BeltData.ts"
      via: "beltDataModel.tools"
      pattern: "beltDataModel\\.tools"
---

<objective>
Create belt-specific tab components (BeltMissionsTab, BeltMissionCard, BeltAnalyticsTab) that consume BeltData.ts and follow existing planet HUD patterns.

Purpose: Provide the tab content components that BeltHUD will render — same two-tab pattern (Missions + Metrics) as planet HUDs per D-03 (same HUD pattern for belt).
Output: Three new component pairs (tsx+css) in Tabs/Belt/, plus index.ts re-exports.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-belt-interaction-content/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/Planetary/data/BeltData.ts:
```typescript
export interface BeltTool {
  id: string;
  name: string;
  category: "llm" | "agent-framework" | "orchestration" | "evaluation" | "rag" | "mcp" | "prompt-engineering";
  proficiency: number; // 0-100
  description: string;
  relatedTechnologies: string[];
}

export interface BeltMissionReport {
  id: string;
  title: string;
  date: string;
  problem: string;
  approach: string;
  outcome: string;
  toolsUsed: string[]; // IDs referencing BeltTool.id
  metrics: { label: string; value: string }[];
}

export const beltDataModel: BeltDataModel; // { name, description, color, tools, missionReports }
```

From src/components/Planetary/UI/Tabs/Tabs.tsx:
```typescript
export interface TabItem {
  id: string;
  label: string;
  content: React.ReactNode;
}
// Tabs component: { tabs: TabItem[], defaultTabId: string, themeColor: string }
```

Pattern reference — AnalyticsTab.tsx uses `getSkillColor(value)` for proficiency coloring:
- ≥90 → #00f0ff, ≥75 → #00ff73, ≥50 → #ffae00, <50 → #ff3366

Pattern reference — MissionsTab.tsx filters MISSION_ARCHIVE by technology overlap, shows error log if empty.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>task 1: create BeltMissionCard component</name>
  <files>src/components/Planetary/UI/Tabs/Belt/BeltMissionCard.tsx, src/components/Planetary/UI/Tabs/Belt/BeltMissionCard.css</files>
  <action>Create BeltMissionCard component that renders a single BeltMissionReport. Follow MissionCard.tsx pattern but adapt for BeltMissionReport shape:

1. Props interface: `{ report: BeltMissionReport, beltColor: string }`
2. Layout structure (per UI-SPEC BeltMissionCard Layout Contract):
   - Header: title (color: beltColor) + year badge (mission-year class)
   - PROBLEMA: section label (monospace, uppercase) + report.problem text
   - ABORDAGEM: section label + report.approach text
   - RESULTADO: section label + report.outcome text
   - Divider (border-top)
   - Ferramentas: map report.toolsUsed to BeltTool names via beltDataModel.tools lookup, render as mission-tag.active spans (all tags active since belt owns these tools)
   - Métricas: map report.metrics as bullet list (label: value)
3. CSS: Create BeltMissionCard.css adapting MissionCard.css pattern:
   - Same .mission-card base class reuse (glassmorphism, backdrop blur, border)
   - border-left: 2px solid beltColor
   - Section labels: monospace, uppercase, small font
   - No external links section (BeltMissionReport has no external_links field)
   - Tags use mission-tag.active styling with beltColor

Use React.FC pattern with explicit interface (convention from CONVENTIONS.md). Import BeltMissionReport type from BeltData.ts.
</action>
<verify>
<automated>npx tsc --noEmit --project tsconfig.app.json 2>&1 | Select-String "BeltMissionCard"</automated>
</verify>
<done>BeltMissionCard.tsx compiles with no errors, renders BeltMissionReport with problem/approach/outcome sections, tools tags, and metrics. CSS file created with belt-specific left border.</done>
</task>

<task type="auto" tdd="false">
  <name>task 2: create BeltMissionsTab and BeltAnalyticsTab components</name>
  <files>src/components/Planetary/UI/Tabs/Belt/BeltMissionsTab.tsx, src/components/Planetary/UI/Tabs/Belt/BeltMissionsTab.css, src/components/Planetary/UI/Tabs/Belt/BeltAnalyticsTab.tsx, src/components/Planetary/UI/Tabs/Belt/BeltAnalyticsTab.css, src/components/Planetary/UI/Tabs/index.ts</files>
  <action>Create two tab components following existing planet tab patterns:

**BeltMissionsTab.tsx** (pattern: MissionsTab.tsx):
1. No props needed — reads directly from beltDataModel
2. Import beltDataModel from BeltData.ts and BeltMissionCard
3. Render beltDataModel.missionReports mapped to BeltMissionCard components with beltColor={beltDataModel.color}
4. Empty state: use mission-error-log class with "[ERRO]: Nenhuma missão registrada nos arquivos." (per UI-SPEC copywriting contract)
5. Wrap in div with className="mission-list" (reuses existing CSS gap spacing)
6. CSS: BeltMissionsTab.css — minimal, may just import/extend mission-list pattern

**BeltAnalyticsTab.tsx** (pattern: AnalyticsTab.tsx):
1. No props needed — reads directly from beltDataModel
2. Import beltDataModel from BeltData.ts
3. Header: "Progresso de Sincronização da Rede S.O.N.D.A." + "STATUS: ATIVA" (beltColor) — per UI-SPEC copywriting
4. Group tools by category: use Array.groupBy or reduce to group beltDataModel.tools by their category field
5. For each category group:
   - Category header (monospace, uppercase): e.g., "LLM", "ORCHESTRATION"
   - For each tool in group:
     - Row header: tool name (left) + proficiency/100 (right, colored by getSkillColor)
     - Progress bar: same pattern as AnalyticsTab (progress-track + progress-fill with animation)
     - Tool description: 14px, 1.6 line-height, muted color (per UI-SPEC)
     - Related technologies: render as alternative-tag elements (same pattern as AnalyticsTab alternatives)
6. Reuse getSkillColor function (copy from AnalyticsTab.tsx — pure function, no side effects)
7. CSS: BeltAnalyticsTab.css — adapt AnalyticsTab.css pattern, same class names for progress bars, rows, alternatives

**Update Tabs/index.ts**: Add exports for BeltMissionsTab and BeltAnalyticsTab:
```typescript
export { BeltMissionsTab } from "./Belt/BeltMissionsTab";
export { BeltAnalyticsTab } from "./Belt/BeltAnalyticsTab";
```

Follow TypeScript strict mode conventions: use `type` imports for interfaces, named exports, functional components with React.FC.
</action>
<verify>
<automated>npx tsc --noEmit --project tsconfig.app.json 2>&1 | Select-String "Belt(Missions|Analytics)Tab"</automated>
</verify>
<done>BeltMissionsTab renders all 3 belt mission reports with BeltMissionCard. BeltAnalyticsTab renders all 10 tools grouped by 7 categories with proficiency bars. Both compile with no errors. Tabs/index.ts exports both components.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static data → DOM | BeltData.ts content rendered directly — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Injection | BeltMissionCard | mitigate | All belt data is static TypeScript constants — no user input flows through. React auto-escapes text content. No dangerouslySetInnerHTML used. |
| T-03-02 | Information Disclosure | BeltAnalyticsTab | accept | Displays only public portfolio skill data — no sensitive information. Static constants only. |
</threat_model>

<verification>
- TypeScript compilation passes with no errors for all new files
- BeltMissionCard renders a BeltMissionReport with all sections visible
- BeltMissionsTab shows all 3 mission reports from beltDataModel
- BeltAnalyticsTab shows all 10 tools grouped by category with correct proficiency colors
- Empty state displays correctly when missionReports array is empty
</verification>

<success_criteria>
- BeltMissionCard, BeltMissionsTab, BeltAnalyticsTab components exist and compile
- BeltMissionsTab consumes beltDataModel.missionReports (3 reports)
- BeltAnalyticsTab consumes beltDataModel.tools (10 tools, 7 categories)
- Tab components follow existing planet HUD patterns (MissionCard, MissionsTab, AnalyticsTab)
- CSS follows existing conventions (plain CSS, co-located files, kebab-case classes)
- Belt color #a9fc03 used consistently for belt-specific styling
</success_criteria>

<output>
After completion, create `.planning/phases/03-belt-interaction-content/03-01-SUMMARY.md`
</output>
